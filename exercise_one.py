# Create .env file with environmental variables:

# username
# password

# Write an application asking user to enter username and password. 

# Check if user entered the same credentials as in your .env file. 
# If so print "ACCESS GRANTED" Otherwise print "WRONG CREDENTIALS" until user enters correct details.